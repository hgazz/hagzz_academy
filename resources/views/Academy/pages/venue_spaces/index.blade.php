@extends('Academy.Layouts.master')
@section('title', trans('admin.venues.spaces'))
@section('content')
<div class="middle-content container-xxl p-0"><div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4"><div><h3 class="mb-1">{{ trans('admin.venues.spaces') }}</h3><p class="text-muted mb-0">{{ trans('admin.venues.spaces_hint') }}</p></div><a href="{{ route('academy.venue-spaces.create') }}" class="btn btn-primary"><i class="fa-solid fa-plus me-1"></i>{{ trans('admin.venues.add_space') }}</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="table-responsive bg-white rounded shadow-sm"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ trans('admin.venues.name') }}</th><th>{{ trans('admin.venues.location') }}</th><th>{{ trans('admin.venues.type') }}</th><th>{{ trans('admin.venues.hourly_price') }}</th><th>{{ trans('admin.venues.working_hours') }}</th><th>{{ trans('admin.actions') }}</th></tr></thead><tbody>
@forelse($spaces as $space)<tr><td class="fw-semibold">{{ $space->name }}</td><td>{{ $space->venue->name }}</td><td>{{ trans('admin.venues.space_types.'.$space->space_type) }}</td><td>{{ number_format($space->hourly_price,2) }} {{ $space->venue->currency }}</td><td>{{ substr($space->opens_at,0,5) }} - {{ substr($space->closes_at,0,5) }}</td><td><div class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('academy.venue-spaces.edit',$space) }}"><i class="fa-solid fa-pen"></i></a><form method="POST" action="{{ route('academy.venue-spaces.destroy',$space) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="fa-solid fa-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">{{ trans('admin.venues.empty_spaces') }}</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $spaces->links() }}</div></div>
@endsection
