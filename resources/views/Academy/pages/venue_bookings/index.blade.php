@extends('Academy.Layouts.master')
@section('title', trans('admin.venues.bookings'))
@section('content')
<div class="middle-content container-xxl p-0"><div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4"><div><h3 class="mb-1">{{ trans('admin.venues.bookings') }}</h3><p class="text-muted mb-0">{{ trans('admin.venues.bookings_hint') }}</p></div><a href="{{ route('academy.venue-bookings.create') }}" class="btn btn-primary"><i class="fa-solid fa-calendar-plus me-1"></i>{{ trans('admin.venues.add_booking') }}</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="table-responsive bg-white rounded shadow-sm"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ trans('admin.venues.reference') }}</th><th>{{ trans('admin.venues.customer') }}</th><th>{{ trans('admin.venues.space') }}</th><th>{{ trans('admin.venues.time') }}</th><th>{{ trans('admin.venues.total') }}</th><th>{{ trans('admin.venues.payment') }}</th><th>{{ trans('admin.status') }}</th><th>{{ trans('admin.actions') }}</th></tr></thead><tbody>
@forelse($bookings as $booking)<tr><td class="fw-semibold">{{ $booking->reference }}</td><td>{{ $booking->customer->name }}<small class="d-block text-muted">{{ $booking->customer->phone }}</small></td><td>{{ $booking->space->name }}<small class="d-block text-muted">{{ $booking->space->venue->name }}</small></td><td>{{ $booking->starts_at->format('Y-m-d') }}<small class="d-block text-muted">{{ $booking->starts_at->format('H:i') }} - {{ $booking->ends_at->format('H:i') }}</small></td><td>{{ number_format($booking->total_amount,2) }}</td><td>{{ number_format($booking->paid_amount,2) }}<small class="d-block text-muted">{{ trans('admin.venues.payment_states.'.$booking->payment_status) }}</small></td><td><span class="badge bg-info">{{ trans('admin.venues.statuses.'.$booking->status) }}</span></td><td><div class="d-flex gap-2"><a class="btn btn-sm btn-outline-success d-inline-flex align-items-center gap-1" target="_blank" href="{{ route('academy.invoices.venues.print',['booking'=>$booking,'paper'=>'a4']) }}" title="{{ app()->getLocale()==='ar'?'طباعة الفاتورة':'Print invoice' }}"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9V2h12v7"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect width="12" height="8" x="6" y="14"/></svg><span>{{ app()->getLocale()==='ar'?'طباعة':'Print' }}</span></a><a class="btn btn-sm btn-outline-primary" href="{{ route('academy.venue-bookings.edit',$booking) }}"><i class="fa-solid fa-pen"></i></a>@if($booking->status!=='cancelled')<form method="POST" action="{{ route('academy.venue-bookings.destroy',$booking) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="{{ trans('admin.venues.cancel_booking') }}"><i class="fa-solid fa-ban"></i></button></form>@endif</div></td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">{{ trans('admin.venues.empty_bookings') }}</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $bookings->links() }}</div></div>
@endsection
